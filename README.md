# 🧽 Spongelang
# 스펀지랭

A middle layer that absorbs, normalizes, and translates semantic structures between languages.  
여러 언어의 의미 구조를 흡수하고 정규화하여 다음 레이어로 전달하는 중간 의미 레이어.

Spongelang is not a full language.  
스펀지랭은 완전한 언어가 아니다.

It acts as a flexible semantic sponge between layers:  
스펀지처럼 레이어 사이의 의미를 받아서 변환하는 역할이다:

- absorbs meaning / 의미 흡수  
- normalizes structures / 구조 정규화  
- resolves conflicts / 의미 충돌 해결  
- re-shapes semantics / 의미 재구성  
- passes to next layer / 다음 레이어로 전달

Spongelang can be inserted anywhere in the Multi-layer Language Pipeline.  
스펀지랭은 멀티 레이어 파이프라인 안에서 **어디든 넣을 수 있다.**

Only the final output (NASM) is fixed — all other layers are flexible.  
마지막 출력(NASM)만 고정, 그 외 레이어 순서는 자유롭다.

Examples:  
예시:

- Ruby → Spongelang → Rust → C++ → NASM  
- Java → Scala → Spongelang → Rust → NASM  
- Haskell → Spongelang → Rust → NASM  
- Rust → Spongelang → C++ → NASM
